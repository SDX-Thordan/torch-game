[package]
name = "torch-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
# cdylib -> the Godot GDExtension; rlib -> native `cargo test` of the pure sim.
crate-type = ["cdylib", "rlib"]

[dependencies]
godot = "0.2"
