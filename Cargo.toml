[workspace]
members = ["crates/torch-core"]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"
publish = false

# Optimize release builds of the deterministic core for the mobile target.
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
